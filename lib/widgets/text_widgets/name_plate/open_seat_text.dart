import 'package:flutter/material.dart';

class OpenSeatText extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text(
      'Open',
      style: TextStyle(fontSize: 11.0),
    );
  }
}
