import 'package:flutter/material.dart';

class ClubMembersDetailedView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Member details View"),
      ),
    );
  }
}
