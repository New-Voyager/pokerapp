import 'package:flutter/cupertino.dart';

class NamePlateView extends StatelessWidget {
  const NamePlateView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
