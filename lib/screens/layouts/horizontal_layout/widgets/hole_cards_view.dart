import 'package:flutter/material.dart';

class HoleCardsView extends StatelessWidget {
  const HoleCardsView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
